import { InviteStatusEmojiPipe } from './invite-status-emoji.pipe';

describe('InviteStatusEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new InviteStatusEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
